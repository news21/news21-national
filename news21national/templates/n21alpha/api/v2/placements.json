{% if callback %}{{callback}}({% endif %}{"placements":[
    {% for placement in placements %}
    { "date_ran":"{{ placement.story_ran|date:"F d, Y" }}", "partner":"{{ placement.partner|escape }}", "partner_headline":"{{ placement.partner.partner_headline|escape }}", "partner_url":"{{ placement.partner.site_url }}", "url":"{{ placement.placement_url }}", "url_active":"{{ placement.url_active }}", "screengrab_url":"{{placement.screengrab_url}}" }{% if not forloop.last %},{% endif %}
    {% endfor %}
]}{% if callback %}){% endif %}